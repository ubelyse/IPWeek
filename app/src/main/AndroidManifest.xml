<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotelreserve">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelReserve">
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.HotelDetailActivity" />
        <activity android:name=".ui.ChosenActivity"></activity>
        <activity android:name=".ui.DailyChoose"></activity>
        <activity android:name=".ui.ChoseMidDay"></activity>
        <activity android:name=".ui.ChoseAfternoon"></activity>
        <activity android:name=".ui.ChoseEvening"></activity>
        <activity android:name=".ui.HotelActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.MainActivity" />
        </activity>
        <activity android:name=".ui.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>